# Backend environment example
# Copy to .env and update DB_PASSWORD (and DB_HOST if needed).

DB_HOST=13.126.209.246
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=changeme
DB_NAME=railway

# Alternatively, use a single connection string:
# DATABASE_URL=postgres://postgres:changeme@13.126.209.246:5432/railway
